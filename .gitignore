node_modules/
.DS_Store
*.local
# PII guard: never commit alias maps or raw client data
alias-map*
*-PII*
client-data/
# Billing is LOCAL-ONLY — never track it in this repo, even if a folder appears here
billing/
billing-local/
*billing-local*
*PRIVATE_DO_NOT_UPLOAD*
.vercel
